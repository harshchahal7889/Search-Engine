# Search-Engine
Semantic Search Engine retrieves files by meaning, not exact filenames. Combines OS (C++ virtual file system, disk scheduling, ACL), DBMS (SQLite/PostgreSQL for metadata + vector storage), and NLP (Sentence-Transformer embeddings, cosine similarity) into one pipeline for intelligent, meaning-based file retrieval.
